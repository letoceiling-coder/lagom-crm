# Инструкция по развертыванию данных на сервере

Эта инструкция поможет вам выполнить все seeders на сервере для загрузки данных продуктов, сервисов, баннеров и всех связанных данных.

## ⚡ Быстрый способ: Использование команды deploy

**Самый простой способ** - использовать команду `php artisan deploy` на локальной машине. Она автоматически:
1. Соберет фронтенд
2. Закоммитит изменения в git
3. Отправит на сервер
4. На сервере выполнит миграции и (опционально) seeders

### Использование:

```bash
# На локальной машине (обычный deploy без seeders)
php artisan deploy --insecure

# С выполнением seeders (включая импорт данных)
php artisan deploy --insecure --with-seed
```

**Важно:** 
- По умолчанию seeders **НЕ выполняются** при deploy (чтобы не замедлять обычные обновления)
- Для выполнения seeders используйте флаг `--with-seed`
- При использовании `--with-seed` будут выполнены все необходимые seeders, включая:
  - Базовые seeders (роли, меню, настройки)
  - Импорт данных продуктов, сервисов и баннеров
  - Регистрацию медиа файлов

---

## 📋 Ручной способ: Пошаговая инструкция

## Предварительные требования

- Доступ к серверу по SSH
- Установленный PHP и Composer
- Настроенная база данных
- Выполненные миграции (`php artisan migrate`)

## Пошаговая инструкция

### Шаг 1: Подключение к серверу

Подключитесь к серверу по SSH:

```bash
ssh user@your-server.com
```

Перейдите в директорию проекта:

```bash
cd /path/to/your/project
```

### Шаг 2: Проверка наличия файла экспорта

Убедитесь, что файл экспорта находится в правильной директории:

```bash
ls -la database/seeders/exports/products_services_export_2025-12-03_150726.json
```

Если файла нет, скопируйте его из локальной машины:

```bash
# С локальной машины (в новом терминале)
scp database/seeders/exports/products_services_export_2025-12-03_150726.json user@your-server.com:/path/to/your/project/database/seeders/exports/
```

### Шаг 3: Проверка настроек базы данных

Убедитесь, что файл `.env` содержит правильные настройки подключения к базе данных:

```bash
cat .env | grep DB_
```

Должны быть указаны:
- `DB_CONNECTION=mysql`
- `DB_HOST=127.0.0.1`
- `DB_PORT=3306`
- `DB_DATABASE=your_database`
- `DB_USERNAME=your_username`
- `DB_PASSWORD=your_password`

### Шаг 4: Проверка выполнения миграций

Убедитесь, что все миграции выполнены:

```bash
php artisan migrate:status
```

Если есть невыполненные миграции, выполните:

```bash
php artisan migrate
```

### Шаг 5: Резервное копирование базы данных (рекомендуется)

**ВАЖНО:** Перед выполнением seeders рекомендуется создать резервную копию базы данных:

```bash
# Создание резервной копии
mysqldump -u your_username -p your_database > backup_$(date +%Y%m%d_%H%M%S).sql
```

Или используйте Laravel Backup (если установлен):

```bash
php artisan backup:run
```

### Шаг 6: Копирование медиа файлов

**ВАЖНО:** Медиа файлы (изображения, иконки) не копируются автоматически. Их нужно скопировать вручную.

На локальной машине найдите все медиа файлы, которые используются в продуктах, сервисах и баннерах:

```bash
# На локальной машине - найти все используемые медиа файлы
# Проверьте пути в экспортированном JSON файле
```

Скопируйте медиа файлы на сервер:

```bash
# С локальной машины
scp -r public/upload/* user@your-server.com:/path/to/your/project/public/upload/
```

Или используйте rsync для более эффективной синхронизации:

```bash
rsync -avz public/upload/ user@your-server.com:/path/to/your/project/public/upload/
```

### Шаг 7: Выполнение базовых seeders

Сначала выполните базовые seeders (роли, меню, настройки):

```bash
php artisan db:seed --class=RoleSeeder
php artisan db:seed --class=MenuSeeder
php artisan db:seed --class=AppCategorySeeder
php artisan db:seed --class=CasesBlockSettingsSeeder
php artisan db:seed --class=HowWorkBlockSettingsSeeder
php artisan db:seed --class=FaqBlockSettingsSeeder
php artisan db:seed --class=WhyChooseUsBlockSettingsSeeder
php artisan db:seed --class=AboutSettingsSeeder
php artisan db:seed --class=ContactSettingsSeeder
php artisan db:seed --class=FooterSettingsSeeder
```

### Шаг 8: Импорт данных продуктов и сервисов

Выполните импорт всех данных из экспортированного JSON файла:

```bash
php artisan db:seed --class=ImportProductsServicesSeeder
```

Или укажите конкретный файл через переменную окружения:

```bash
IMPORT_FILE=database/seeders/exports/products_services_export_2025-12-03_150726.json php artisan db:seed --class=ImportProductsServicesSeeder
```

**Ожидаемый вывод:**

```
🚀 Начало импорта данных из файла: database/seeders/exports/products_services_export_2025-12-03_150726.json
📊 Версия экспорта: 1.0
📅 Дата экспорта: 2025-12-03 15:07:26
📁 Импорт разделов...
✅ Импортировано разделов: 2
📋 Импорт опций...
✅ Импортировано опций: 10
🌳 Импорт деревьев опций...
✅ Импортировано деревьев опций: 213
📦 Импорт экземпляров...
✅ Импортировано экземпляров: 12
🖼️ Импорт информации о медиа файлах...
✅ Импортировано медиа файлов: X
💼 Импорт услуг...
✅ Импортировано услуг: 13
🛍️ Импорт продуктов...
✅ Импортировано продуктов: 27
🎨 Импорт баннеров...
✅ Импортировано баннеров: 1
🔗 Импорт связей...
✅ Импортировано связей product_service: 350
✅ Импортировано связей option_service: 0
✅ Импортировано связей option_tree_service: 11
✅ Импортировано связей instance_service: 0
✅ Импорт завершен успешно!
```

### Шаг 9: Регистрация медиа файлов

После импорта данных зарегистрируйте все медиа файлы в таблице `media`:

```bash
php artisan db:seed --class=RegisterAllMediaFilesSeeder
```

### Шаг 10: Обновление папок медиа файлов

Обновите информацию о папках для всех медиа файлов:

```bash
php artisan db:seed --class=UpdateMediaFolderSeeder
```

### Шаг 11: Проверка результатов

Проверьте, что данные успешно импортированы:

```bash
# Проверка количества записей
php artisan tinker
```

В tinker выполните:

```php
// Проверка разделов
\App\Models\Chapter::count();
// Должно быть: 2

// Проверка продуктов
\App\Models\Product::count();
// Должно быть: 27

// Проверка услуг
\App\Models\Service::count();
// Должно быть: 13

// Проверка баннеров
\App\Models\Banner::count();
// Должно быть: 1

// Проверка связей
\DB::table('product_service')->count();
// Должно быть: 350

exit
```

### Шаг 12: Очистка кеша

Очистите все кеши Laravel:

```bash
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

### Шаг 13: Проверка на сайте

Откройте сайт в браузере и проверьте:

1. Главная страница - должен отображаться баннер
2. Страница продуктов - должны отображаться все продукты
3. Страница услуг - должны отображаться все услуги
4. Проверьте, что изображения загружаются корректно

## Решение проблем

### Ошибка: "File not found"

Если файл экспорта не найден:

```bash
# Проверьте путь к файлу
ls -la database/seeders/exports/

# Убедитесь, что файл существует
cat database/seeders/exports/products_services_export_2025-12-03_150726.json | head -20
```

### Ошибка: "Duplicate entry"

Если возникают ошибки дублирования записей, это нормально - seeder использует `updateOrCreate`, который обновляет существующие записи.

### Ошибка: "Media file not found"

Если изображения не отображаются:

1. Проверьте, что медиа файлы скопированы на сервер
2. Проверьте права доступа к директории `public/upload`:

```bash
chmod -R 755 public/upload
chown -R www-data:www-data public/upload
```

3. Проверьте пути к файлам в базе данных

### Ошибка: "Foreign key constraint"

Если возникают ошибки внешних ключей:

1. Убедитесь, что все миграции выполнены
2. Проверьте порядок импорта данных (он должен быть правильным)

## Полная команда для выполнения всех seeders

Если вы хотите выполнить все seeders одной командой:

```bash
php artisan db:seed
```

Но это выполнит все seeders, включая те, которые могут быть не нужны. Рекомендуется выполнять seeders по отдельности, как описано выше.

## Альтернативный способ: выполнение через один скрипт

Создайте файл `deploy-seeders.sh`:

```bash
#!/bin/bash

echo "🚀 Начало развертывания данных..."

# Базовые seeders
php artisan db:seed --class=RoleSeeder
php artisan db:seed --class=MenuSeeder
php artisan db:seed --class=AppCategorySeeder
php artisan db:seed --class=CasesBlockSettingsSeeder
php artisan db:seed --class=HowWorkBlockSettingsSeeder
php artisan db:seed --class=FaqBlockSettingsSeeder
php artisan db:seed --class=WhyChooseUsBlockSettingsSeeder
php artisan db:seed --class=AboutSettingsSeeder
php artisan db:seed --class=ContactSettingsSeeder
php artisan db:seed --class=FooterSettingsSeeder

# Импорт данных
php artisan db:seed --class=ImportProductsServicesSeeder

# Регистрация медиа файлов
php artisan db:seed --class=RegisterAllMediaFilesSeeder
php artisan db:seed --class=UpdateMediaFolderSeeder

# Очистка кеша
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear

echo "✅ Развертывание завершено!"
```

Сделайте файл исполняемым и запустите:

```bash
chmod +x deploy-seeders.sh
./deploy-seeders.sh
```

## Важные замечания

1. **Резервное копирование**: Всегда создавайте резервную копию базы данных перед выполнением seeders
2. **Медиа файлы**: Не забывайте копировать медиа файлы на сервер
3. **Права доступа**: Убедитесь, что веб-сервер имеет права на чтение медиа файлов
4. **Порядок выполнения**: Соблюдайте порядок выполнения seeders, указанный в инструкции
5. **Проверка**: После выполнения проверьте результаты в базе данных и на сайте

## Контакты для поддержки

Если возникли проблемы, проверьте логи Laravel:

```bash
tail -f storage/logs/laravel.log
```

